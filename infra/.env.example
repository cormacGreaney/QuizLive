# MySQL
MYSQL_ROOT_PASSWORD=password
MYSQL_DATABASE=quiz_auth

# Auth Service (Spring)
DB_USERNAME=root
DB_PASSWORD=password
JWT_SECRET=

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Allowed redirect URIs (two variants to avoid slash mismatch)
GOOGLE_ALLOWED_REDIRECT_URIS=http://localhost:5173/oauth2/callback/google,http://localhost:5173/oauth2/callback/google/

# Auth JDBC URL (inside Docker: host is 'mysql')
SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/quiz_auth?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
